# Telegram Bot Project

This is a modular Telegram bot for ordering products with ZarinPal payment integration.

## Setup
1. Create a virtual environment: `python -m venv venv`
2. Activate venv and install requirements: `pip install -r requirements.txt`
3. Configure `config.py` with your tokens and BASE_URL
